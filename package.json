{
    "private": true,
    "name": "portfolio-v3",
    "version": "0.0.1",
    "author": {
        "name": "Abraham Anak Agung",
        "email": "abraham.agel@outlook.com",
        "url": "https://anakagung.com"
    },
    "license": "MIT",
    "scripts": {
        "build": "lerna run build --parallel",
        "build-studio": "lerna bootstrap && (cd portfolioblog && npm run build)",
        "build-web": "lerna bootstrap && (cd web && npm run export)",
        "dev": "lerna run dev --parallel",
        "format": "lerna run format",
        "graphql-deploy": "lerna run graphql-deploy",
        "lint": "lerna run lint",
        "postinstall": "lerna bootstrap",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@sanity/cli": "^2.0.0",
        "lerna": "^3.14.0"
    }
}
